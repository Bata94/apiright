version: "2"
sql:
  - engine: "sqlite"
    queries: "queries/"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "gen/go"
        sql_package: "database/sql"
        emit_json_tags: true
